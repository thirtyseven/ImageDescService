<!DOCTYPE html>
<html>
<head>
  <title>Poet Image Description</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>

  <%= csrf_meta_tag %>
</head>

  <body class="index admin_books logged_in">
     <div id="wrapper">
       <div id="header">
         <h1 id="site_title"><a title="Poet, version <%= t(:version)%>, home page" href="/">Poet Image Description</a></h1>
         <ul class="tabbed_navigation" id="tabs"></ul>       
         <p id="utility_nav">
           <% if can? :view_admin, @all %> 
             <%= link_to "Books", admin_books_path %> &nbsp;
           <% else %>
             <%#= link_to "Books", book_list_path %> &nbsp;
           <% end %>
          <%= link_to "Guidelines", edit_book_description_guidance_path %> &nbsp;
          <%= link_to "Help", edit_book_help_path %> &nbsp;
          <% if current_user %>
           <%= link_to 'User Profile', edit_user_path(current_user) %> &nbsp;
          <% end %>
          <% if current_user && ENV['SHOW_MY_BOOKS'] === "true"%>
            <%= link_to 'My Books', book_list_by_user_path %> &nbsp;
          <% end %>
          <% if can? :view_admin, @all %> 
             <%= link_to "Admin", admin_dashboard_path %> &nbsp;
          <% end %>
          <% if user_signed_in? %>
             <%= current_user.username %> 
            <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
          <% else %>
           <%= link_to 'Register', new_user_registration_path %> or <%= link_to 'Sign in', new_user_session_path %>
          <% end %>
          </p>
       </div>
     </div>

       <%= render "shared/error_display" %>
       <%= yield %>

   <div id="poet_footer"> <!-- TODO -- footer font smaller !-->
    <p>Copyright &copy; 2012 <a href="http://www.benetech.org">Benetech</a>.</p> 
    <p><strong>Legal Disclaimer</strong>: The Poet tool is intended to assist in the creation of accessible content. Use of the Poet tool is limited to activities for creating content that comply with applicable copyright law and contractual agreements.</p>
   </div>
     
   </body>    
  
</html>
