 	<frameset onload='document.getElementById("content").focus()'cols='300px, *'>
		<frame name='side_bar' src='/edit_book/side_bar<%= "?book_id=#{params[:book_id]}" %>'></frame>
		<frame name='content' src='/edit_book/content#top<%= "?book_id=#{params[:book_id]}" %>'></frame>
		<noframes>Frames are required for this feature to work.</noframes>
	</frameset>	
	<noframes>Frames are required for this feature to work.</noframes>
 